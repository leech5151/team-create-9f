import type { Tier } from './types';

/** Tier accent colours — verbatim from the design prototype's `T` map. */
export const TIER_COLOR: Record<Tier, string> = {
  1: '#1F5FE0',
  2: '#0E9D8B',
  3: '#E0A200',
};

export const ACCENT = '#FF4A21';
